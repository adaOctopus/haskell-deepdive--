module Final where

awesome = ["Papuchon", "curry", ":)"]
also = ["Quake", "The Simons"]
allAwesome = [awesome, also]

-- length :: [a] -> Int

-- reverse :: [a] -> [a]
-- reverse "blah"
-- "halb"

isPalindrome :: (Eq a) => [a] -> Bool
isPalindrome x = if anapoda == x then True else False
  where
    anapoda = reverse x

x = (+)
fulca xs = (+) (w) 1
  where w = length xs
